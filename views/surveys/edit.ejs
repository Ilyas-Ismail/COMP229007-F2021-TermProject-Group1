<%- include ../partials/header.ejs %>

<div class="container">
    <div class="row">
        <div class="offset-md-3 col-md-6">
            <h1><%= title %></h1>

                <div class="form-group">

                    <h2>Survey Title: <%= surveys.Title %></h2>
                </div>
                <br/>
                <br/>
                <br/>
                <% for(let i = 0; i < surveys.Questions.length; i++) { %>
                <div class="form-group">
                    <h5>Q: <%= surveys.Questions[i] %></h4>
                </div>
                <br/>
                <div class="container">
                <% for(let j = 0; j < surveys.Choices[i].length; j++) { %>
                <div class="form-group">
                    <input type="radio" id="choice<%= i %>" name="choice<%= i %>" value="<%= j %>">
                    <label for="choice<%= i %>">
                        <%= surveys.Choices[i][j] %>
                    </label>
                </div>
                <br />
                <% } %>
                </div>
                <br/>
                <button class="btn btn-primary" type="submit" name="btn" value="done">
                    <i class="fas fa-edit"></i> 
                    Edit this question
                </button>
                <% } %>

                <a href="/surveys/list" class="btn btn-primary">
                    <i class="fas fa-edit"></i>
                    Submit
                </a>
                <a href="/surveys/list" class="btn btn-warning">
                    <i class="fas fa-undo"></i> 
                    Return
                </a>

            </form>
        </div>

    </div>
</div>

<%- include ../partials/footer.ejs %>